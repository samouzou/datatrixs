
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents an individual user within the Datatrixs application, linked to their external authentication identity and application-specific roles.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "externalAuthIdentifier": {
          "type": "string",
          "description": "Unique identifier provided by the external authentication system."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the user."
        },
        "role": {
          "type": "string",
          "description": "The application-specific role assigned to the user."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "externalAuthIdentifier",
        "email",
        "firstName",
        "lastName",
        "role",
        "createdAt",
        "updatedAt"
      ]
    },
    "Company": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Company",
      "type": "object",
      "description": "Represents a private equity holding company, acting as the umbrella organization for multiple retail locations.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Company entity."
        },
        "name": {
          "type": "string",
          "description": "The official name of the holding company."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the holding company."
        },
        "members": {
          "type": "object",
          "description": "A map of user IDs to roles ('admin' or 'member').",
          "additionalProperties": {
            "type": "string",
            "enum": [
              "admin",
              "member"
            ]
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the company record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the company record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "createdAt",
        "updatedAt"
      ]
    },
    "CompanyInvitation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CompanyInvitation",
      "type": "object",
      "description": "Represents an invitation sent to a user to join a holding company.",
      "properties": {
        "id": {
          "type": "string"
        },
        "companyId": {
          "type": "string"
        },
        "companyName": {
          "type": "string"
        },
        "email": {
          "type": "string",
          "format": "email"
        },
        "role": {
          "type": "string",
          "enum": [
            "admin",
            "member"
          ]
        },
        "invitedBy": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "accepted",
            "declined"
          ]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "companyId",
        "companyName",
        "email",
        "role",
        "status",
        "createdAt"
      ]
    },
    "Location": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Location",
      "type": "object",
      "description": "Represents an individual retail unit managed by a holding company.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Location entity."
        },
        "companyId": {
          "type": "string",
          "description": "Reference to the Company that owns this location."
        },
        "name": {
          "type": "string",
          "description": "The name of the retail location."
        },
        "addressLine1": {
          "type": "string",
          "description": "The primary street address of the location."
        },
        "addressLine2": {
          "type": "string",
          "description": "Secondary address information for the location."
        },
        "city": {
          "type": "string",
          "description": "The city where the location is situated."
        },
        "state": {
          "type": "string",
          "description": "The state or province where the location is situated."
        },
        "zipCode": {
          "type": "string",
          "description": "The postal code of the location."
        },
        "phoneNumber": {
          "type": "string",
          "description": "The main contact phone number for the location."
        },
        "companyMembers": {
          "type": "object",
          "description": "Denormalized membership map from the parent company.",
          "additionalProperties": {
            "type": "string",
            "enum": [
              "admin",
              "member"
            ]
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the location record was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the location record was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "companyId",
        "name",
        "addressLine1",
        "city",
        "state",
        "zipCode",
        "phoneNumber",
        "createdAt",
        "updatedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles."
        }
      },
      {
        "path": "/companies/{companyId}",
        "definition": {
          "entityName": "Company",
          "schema": {
            "$ref": "#/backend/entities/Company"
          },
          "description": "Stores holding company information."
        }
      },
      {
        "path": "/company_invitations/{invitationId}",
        "definition": {
          "entityName": "CompanyInvitation",
          "schema": {
            "$ref": "#/backend/entities/CompanyInvitation"
          },
          "description": "Stores invitations sent to join companies."
        }
      },
      {
        "path": "/locations/{locationId}",
        "definition": {
          "entityName": "Location",
          "schema": {
            "$ref": "#/backend/entities/Location"
          },
          "description": "Stores individual retail location information."
        }
      }
    ]
  }
}
